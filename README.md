# AI-SQL-Bot
An end-to-end natural-language-to-SQL assistant that lets business users query a MySQL retail database in plain English and get precise answers—no SQL required. The app uses a large language model to generate safe, scoped SQL, executes it against an inventory/sales schema, and returns concise, human-readable results in a Streamlit UI.
